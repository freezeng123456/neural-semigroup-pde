# 新方程实验：部分完成，存储阻塞

训练源码固定为 68939da83ad7de8e61f05c448a370b9a967bffc1。采用非线性扩散和带阻尼 sine-Gordon 方程，各 3 个独立种子、无噪声/1% 噪声、自主/请求时长模型，共 24 个 cell；每 cell 固定 5,000 次更新，最多并发 4 张独立 GPU。没有根据测试结果调整方程系数、学习率、种子或更新预算。

## 当前完成证据

GPU 冒烟作业 23791337 正常完成。正式数组 23791338 的 cell 000–003 为 COMPLETED 0:0；004–007 为 FAILED 120:0；008–023 为 FAILED 0:53，未正常启动。当前没有本轮 GPU 任务运行，未提交修复重跑。

共享主目录 /work/home/zenghang 的 50 GB 配额已满，可用空间为 0。touch 和 tar 均报 No space left on device；归档收集器未能写入，也没有启动。仅完成 4/24，不能计算预先规定的完整三种子比较或宣称新方程效果更好。

原始目录仍保留于 /work/home/zenghang/semigroup-new-equations-20260910-r1。login03 临时盘已生成原始目录副本，路径、哈希见 LAUNCH_RECEIPT.json。该副本尚未下载，不能当作本地已回收。

已把上一轮在本地完整备份、哈希一致的约 26 MB 源码压缩包迁至 login03 临时盘；原始结果未删除。迁移后主目录仍不可写。不要把登录节点 /tmp 当作 GPU 共享存储。随后把已结束实验的旧 bootstrap 迁至 login04:/tmp/zenghang-semigroup-evacuation-login04-20260910-r1/confirmation-bootstrap；命令成功返回，但共享主目录仍显示可用空间 0，打包再次失败。

## 校验覆盖

- 已通过：方程实现单元检查、参考解时间步细化检查、GPU 冒烟；训练源代码与上传压缩包哈希锁定。
- 调度正常结束：4/24 个正式 cell；这不是全矩阵完成或独立检查点审计通过。
- 失败：4 个执行任务，16 个启动任务。
- 受阻：原始结果本地回收、失败检查点可用性审计、后续正式任务、完整 120 个检查点及 96 个 CPU 主端点重放。
- 未运行：完整新矩阵结果汇总与主结果报告；不存在已确认的新方程优势。

## 恢复条件

恢复共享目录的可靠可写空间后，先回收并审计原始 r1，核对失败 cell 的更新日志与检查点，区分可免训练补评估和需要重跑的任务。其余未启动任务按相同冻结协议执行，使用唯一的新输出根；不得覆盖失败现场，也不得自动扩大计算预算。

同目录 confirmation/ 是上一轮独立确认的完整结果；其结果与本轮新方程严格分开。

## 中断检查点目录核查

000–003 各有 5 个非空权重文件及 done/summary。004 保留非空 final 和 fixed_005000，但缺少 summary/done，需要进一步验证和补评估。005–007 的 best.pt 为 0 字节，仅 initial/fixed_002000 非空；没有 final/fixed_005000，不能算完整训练。

004 的 initial/best/final/fixed_005000 已在远端 CPU 成功加载，全部有限；final 与 fixed_005000 完全相等，并相对 initial 发生变化。该 cell 具备免重训补评估的条件，尚未完成补评估。最终 squeue 为空。
